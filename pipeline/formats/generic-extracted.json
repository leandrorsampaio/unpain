{
  "name": "generic-extracted",
  "signature": ["date", "amount", "currency", "counterparty", "purpose"],
  "delimiter": ",",
  "decimal": "dot",
  "date_format": "%Y-%m-%d",
  "_note": "Normalized extractor output, never a bank-authored export. Every production ingestion path requires a reconciliation report for this format, regardless of filename. Deterministic PDF extractors are admitted directly after pipeline/extraction.py rechecks opening + rows == closing over the exact CSV being imported.",
  "columns": {
    "date": "date",
    "amount": "amount",
    "currency": "currency",
    "counterparty": "counterparty",
    "purpose": "purpose",
    "iban": "counterparty_iban",
    "force_review": "force_review"
  }
}
